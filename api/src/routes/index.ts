import { route as eventsRoutes } from "./events";
import { route as userRoutes } from "./user";
import { route as voucherRoutes } from "./events";

export const routes = {
  userRoutes,
  eventsRoutes,
  voucherRoutes,
};
